import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ipaty',
  appName: 'iPaty',
  webDir: 'app/dist',
  bundledWebRuntime: false,
};

export default config;
